var assert = {
  isTrue: function(note.text(string) === string) {
    if(!note.text(string) === string) {
    throw new Error("Assertion failed: " + note.text(string) === string + " is not truthy" );
    }
  }
}
